# не опускать ниже: 0.115.6 тянул starlette 0.41.3 с восемью известными
# уязвимостями (PYSEC-2026-161/248/249/1941/1942/2280/2281). В этом коде ни один
# из уязвимых путей не используется (нет StaticFiles, FileResponse, UploadFile,
# Form, HTTPEndpoint), но держать их в прод-образе незачем — здесь starlette 1.3.1
fastapi==0.141.1
uvicorn[standard]==0.51.0
SQLAlchemy==2.0.52
alembic==1.19.1
psycopg2-binary==2.9.12
pydantic-settings==2.14.2
email-validator==2.3.0
python-jose[cryptography]==3.5.0
# прямое использование bcrypt (passlib удалён: не поддерживается и несовместим с bcrypt>=4.1)
bcrypt==5.0.0
# py-vapid (зависимость pywebpush) требует cryptography>=46; не опускать ниже
# 50.0.0 — в 49.0.0 PYSEC-2026-3552, на нём падает гейт pip-audit
cryptography==50.0.0
PyYAML==6.0.3
aio-pika==10.0.1
pika==1.4.4
httpx==0.28.1
pywebpush==2.4.0
prometheus-client==0.25.0
sentry-sdk==2.67.1
pytest==9.1.1
pytest-asyncio==1.4.0
playwright==1.61.0
