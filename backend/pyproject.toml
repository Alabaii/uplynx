[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]
# pytest-asyncio 1.x: скоуп event loop фиксируем явно (иначе предупреждение
# и риск смены поведения между версиями); function — как и было де-факто
asyncio_default_fixture_loop_scope = "function"

[tool.ruff]
line-length = 100
